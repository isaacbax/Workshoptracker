using System.Windows;

namespace DesignSheet
{
    public partial class App : Application
    {
        // No custom startup logic required because StartupUri is used.
        // If you later want to control the flow (e.g. show Login then Main),
        // you can remove StartupUri from App.xaml and override OnStartup here.
    }
}
