<Window x:Class="DesignSheet.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign in to DesignData"
        Height="280"
        Width="420"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True">

    <Border CornerRadius="12" Background="White" SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect Color="#000000"
                              Opacity="0.25"
                              ShadowDepth="4"
                              BlurRadius="14"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0"
                    Background="#0f766e"
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseDown">
                <DockPanel LastChildFill="False" Margin="12,0">
                    <TextBlock Text="Workshop Tracker Login"
                               VerticalAlignment="Center"
                               Foreground="White"
                               FontWeight="SemiBold"
                               FontSize="16"/>
                    <StackPanel DockPanel.Dock="Right"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button Width="28" Height="22" Margin="4,8,0,8"
                                Click="MinimizeButton_Click"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                Foreground="White"
                                FontSize="14"
                                Cursor="Hand">_</Button>
                        <Button Width="28" Height="22" Margin="4,8,0,8"
                                Click="CloseButton_Click"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                Foreground="White"
                                FontSize="14"
                                Cursor="Hand">X</Button>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Username" Margin="0,0,0,4"/>
                <TextBox x:Name="UserTextBox"
                         Grid.Row="1"
                         Height="26"
                         FontSize="13"/>

                <TextBlock Text="Password" Grid.Row="2" Margin="0,10,0,4"/>
                <PasswordBox x:Name="PasswordBox"
                             Grid.Row="3"
                             Height="26"
                             FontSize="13"/>

                <StackPanel Grid.Row="4" Orientation="Vertical" Margin="0,10,0,0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Branch" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="BranchComboBox"
                                  Width="180"
                                  Height="26"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Connect"
                                Width="90"
                                Height="30"
                                Click="Connect_Click"
                                Background="#0f766e"
                                Foreground="White"
                                BorderBrush="Transparent"
                                Cursor="Hand"
                                Margin="0,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
